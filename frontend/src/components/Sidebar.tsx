/**
 * Sidebar.tsx — Legacy component (no longer used)
 * Navigation is now handled inline in App.tsx using the state-based tab system.
 * This file is retained for reference only.
 */
export default function Sidebar() {
  return null;
}
